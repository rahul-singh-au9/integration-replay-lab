# No model keys, paid APIs, or frontend secrets are required.
# Optional: BASE_URL=https://your-worker.workers.dev for deployed browser tests.
# Run `npx wrangler login` locally to authorize deployment.
